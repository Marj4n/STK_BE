version: "3.9"

services:
  mysql:
    image: mysql:8.0
    container_name: menu-mysql
    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: menu_db
      MYSQL_USER: admin
      MYSQL_PASSWORD: admin123
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build: .
    container_name: menu-backend
    ports:
      - "3000:3000"
    env_file:
      - .env
    depends_on:
      mysql:
        condition: service_healthy
    # Development: mount only src for live reload
    volumes:
      - ./src:/app/src:cached
      - /app/node_modules
      - /app/dist
    command: >
      sh -c "npm install && npx prisma generate && npm run start:dev"

volumes:
  mysql-data:
